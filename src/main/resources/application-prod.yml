spring:
  profiles:
    include: s3  # ✅ S3 관련 Bean만 등록되게
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate             # 운영은 반드시 validate (자동 변경 금지)
    show-sql: false                  # 운영에서는 SQL 출력 비활성화
  #    properties:
  #      hibernate:
  #        format_sql: true

  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST}
      port: ${SPRING_DATA_REDIS_PORT}
      username: ${SPRING_DATA_REDIS_USERNAME}
      password: ${SPRING_DATA_REDIS_PASSWORD}

supabase: # s3로 마이그레이션 시 주석 처리 - 운영 배포 전 (Supabase 사용)
  url: ${SUPABASE_URL}
  key: ${SUPABASE_KEY}
  profile-bucket: ${SUPABASE_PROFILE_BUCKET}
  poster-bucket: ${SUPABASE_POSTER_BUCKET}
  docs-bucket: ${SUPABASE_DOCS_BUCKET}

# s3로 마이그레이션 진행 시 아래 주석 해제 - 마이그레이션 후 (S3 사용)
#cloud:
#  aws:
#    credentials:
#      access-key: YOUR_AWS_ACCESS_KEY_ID         # 🔒 AWS IAM 사용자 Access Key
#      secret-key: YOUR_AWS_SECRET_ACCESS_KEY     # 🔒 AWS IAM 사용자 Secret Key
#    region:
#      static: ap-northeast-2                     # 예시: 서울 리전 (본인 환경에 맞게 변경)
#    s3:
#      bucket: ticketmon-prod-assets         # [권장] 단일 버킷
#      # prefix(폴더) 기준 관리, supabase → s3 마이그레이션에 최적화
#      profile-prefix: profile-imgs/        # ex) profile-imgs/{userId}.jpg
#      poster-prefix: poster-imgs/          # ex) poster-imgs/{posterId}.jpg
#      seller-docs-prefix: seller-docs/     # ex) seller-docs/{uuid}.pdf
#      # 실제 Key: {prefix}{파일명}
#      # 운영 배포시 prod-assets, dev는 dev-assets 등 환경 분리                # S3 버킷 이름 (개발/운영 구분 가능)

jwt:
  secret: ${JWT_SECRET_KEY}
  expiration: ${JWT_EXPIRATION_MS}

aws:
  sqs:
    endpoint: ${SQS_ENDPOINT}
    access-key: ${AWS_ACCESS_KEY}
    secret-key: ${AWS_SECRET_KEY}

logging:
  level:
    root: WARN
    #    com.team03: INFO              # 운영 로그는 최소한만 출력
    com.team03: WARN              # INFO보다 더 낮춰도 괜찮음
    # -> 실시간 모니터링 툴이 있지 않다면, 불필요한 로그까지 나오는 걸 방지할 수 있음

swagger:
  enabled: false                    # 운영에서는 Swagger 비공개