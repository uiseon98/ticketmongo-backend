# Spring
# 현재 실행할 Spring 프로필 (dev 또는 prod)
SPRING_PROFILES_ACTIVE=dev  # 💡 로컬 개발 시 dev, 운영 배포 시 prod로 변경

# 로그 레벨 (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO  # ✅ 선택사항: 운영 중 로그레벨 제어에 도움

# 서버 포트 (Spring Boot 기본 8080)
SERVER_PORT=8080

# MySQL (Aiven용)    // 실제 키값 넣을 예정
DB_URL=jdbc:mysql://<your-aiven-host>:<port>/ticketing_db
DB_USERNAME=<your-aiven-username>
DB_PASSWORD=<your-aiven-password>

# Redis (Docker용)
SPRING_DATA_REDIS_HOST=localhost
SPRING_DATA_REDIS_PORT=6379
SPRING_DATA_REDIS_USERNAME=default
SPRING_DATA_REDIS_PASSWORD=

# Supabase (프로필 이미지 업로드 등)    // 실제 키값 넣을 예정
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-secret-key
SUPABASE_PROFILE_BUCKET=your-profile-bucket
SUPABASE_POSTER_BUCKET=your-poster-bucket
SUPABASE_DOCS_BUCKET=your-docs-bucket

## ☁️ AWS S3 설정 (🚨 prod에서 사용, dev는 주석처리 권장)
#AWS_ACCESS_KEY=your-aws-access-key
#AWS_SECRET_KEY=your-aws-secret-key
#AWS_REGION=ap-northeast-2
#S3_BUCKET=ticketmon-prod-assets
#
## 📁 Prefix 설정 (S3에서는 버킷 내 폴더처럼 작동)
#S3_PROFILE_PREFIX=profile-imgs/
#S3_POSTER_PREFIX=poster-imgs/
#S3_SELLER_DOCS_PREFIX=seller-docs/

# GitHub Packages 인증용 (CI/CD에서 사용 - storage-java 다운로드 시 사용 -> Supabase SDK 필요 시에만 사용(S3로 배포 시 사용안함))
# GITHUB_PACKAGES_USER=your-github-username
# GITHUB_PACKAGES_TOKEN=ghp_xxxxxxxx

# JWT (인증 토큰 발급/검증용, 로그인 담당자가 추후 교체 예정)
JWT_SECRET_KEY=your-super-secret-key
JWT_EXPIRATION_MS=3600000   # 기본 1시간 (1000 * 60 * 60)

# LocalStack (Docker용) - 운영 환경에서는 access-key, secret-key는 AWS IAM 키로 교체 예정
SQS_ENDPOINT=http://localhost:4566
AWS_ACCESS_KEY=test-key
AWS_SECRET_KEY=test-secret

# WebSocket 용 포트 (추후 도전용)
WEBSOCKET_PORT=8081